# next-auth declares nodemailer@^7 as a peerOptional, while this project
# intentionally uses nodemailer@9. That's fine — next-auth only touches
# nodemailer for its own email magic-link provider, which we don't use.
# Tell npm not to fail the install on this optional mismatch.
legacy-peer-deps=true
